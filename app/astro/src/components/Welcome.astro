---
import astroLogo from '../assets/astro.svg';
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />

	<main>
		<section id="hero">
			<a href="https://astro.build">
				<img src={astroLogo.src} width="115" height="48" alt="Astro" />
			</a>

			<h1 class="headline">
				astro-spa is live <span class="tag">007</span>
			</h1>
			<p class="sub">
				Deployed to AWS EC2 (Nginx) via Terraform + GitHub Actions. This is your home page—edit
				<code>src/pages/index.astro</code> or this component at
				<code>src/components/Welcome.astro</code> and push to <code>main</code> to redeploy.
			</p>

			<div id="links">
				<a class="button" href="https://docs.astro.build">Astro Docs</a>
				<a class="ghost" href="https://github.com/donzpixelz/astro-spa/actions">View Deploy Runs</a>
			</div>
		</section>
	</main>

	<a href="https://astro.build/blog/" id="news" class="box">
		<h2>What’s new in Astro?</h2>
		<p>Explore features, updates, and guides from the Astro team.</p>
	</a>
</div>

<style>
	:root {
		--ink: #111827;
		--brand: #3245ff;
		--brand2: #bc52ee;
	}
	#background {
		position: fixed; inset: 0;
		width: 100%; height: 100%;
		z-index: -1; filter: blur(100px);
	}
	#container {
		font-family: Inter, Roboto, "Helvetica Neue", Arial, sans-serif;
		min-height: 60vh;
	}
	main {
		display: grid; place-items: center; padding: 24px;
	}
	#hero {
		display: grid; gap: 12px;
		padding: 16px 0; max-width: 720px;
	}
	.headline {
		font-size: 28px; margin: 0.2em 0 0.1em;
	}
	.tag {
		display: inline-block;
		font-size: 18px;
		margin-left: 8px;
		padding: 2px 8px;
		border-radius: 999px;
		background: linear-gradient(83deg, var(--brand), var(--brand2));
		color: white;
	}
	.sub {
		color: #4b5563; margin: 0 0 8px;
	}
	code {
		display: inline-block;
		background: #f5f5f7;
		border: 1px solid #e6e7eb;
		border-radius: 8px;
		padding: 2px 6px;
	}
	#links { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 6px; }
	#links a {
		display: inline-flex; align-items: center; gap: 8px;
		text-decoration: none; border-radius: 10px; padding: 10px 14px;
	}
	#links a.button {
		color: #fff;
		background: linear-gradient(83deg, var(--brand), var(--brand2));
		box-shadow: inset 0 -2px 0 rgba(0,0,0,0.18);
	}
	#links a.ghost {
		color: var(--ink); background: #fff; border: 1px solid #e6e7eb;
	}
	.box {
		position: fixed; right: 16px; bottom: 16px;
		max-width: 320px; padding: 14px; border-radius: 14px;
		background: rgba(255,255,255,0.92); border: 1px solid #fff;
		text-decoration: none; color: var(--ink);
		backdrop-filter: blur(18px);
	}
	.box h2 { margin: 0 0 6px; font-size: 18px; }
	.box p { margin: 0; color: #4b5563; }
	@media (max-width: 768px) {
		.box { position: static; display: block; margin: 16px; }
	}
</style>
